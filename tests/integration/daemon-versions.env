# Bitcoin Core and Elements versions for integration tests.
# Busts CI daemon cache only when these change (not when tests/run.sh changes).
# Keep in sync with defaults in tests/run.sh (BITCOIN_REVISION / ELEMENTS_REVISION).
BITCOIN_REVISION=30.2
ELEMENTS_REVISION=23.3.2
